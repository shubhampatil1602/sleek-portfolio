---
title: "The Document Object Model (DOM) in JavaScript"
description: "The DOM represents the structure of web pages. Learn how to manipulate HTML elements using JavaScript."
image: "/shubham.png"
date: "2025-05-28"
---

# DOM Basics in JavaScript

The DOM is a programming interface for web documents. JavaScript uses the DOM to manipulate HTML and CSS.

## Selecting Elements

```js
const heading = document.getElementById("title");
const items = document.querySelectorAll(".item");
```

## Changing Content

```js
heading.textContent = "Updated Title";
```

## Event Listeners

```js
button.addEventListener("click", () => {
  alert("Button Clicked!");
});
```
